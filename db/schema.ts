import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const submissions=sqliteTable('submissions',{id:text('id').primaryKey(),requestId:text('request_id').notNull().unique(),studentName:text('student_name').notNull(),grade:text('grade',{enum:['GRADE_8','GRADE_9','GRADE_10']}).notNull(),studyDay:text('study_day',{enum:['MONDAY','TUESDAY','FRIDAY','SATURDAY']}).notNull(),submittedAt:text('submitted_at').notNull(),updatedAt:text('updated_at').notNull(),storageKey:text('storage_key').notNull().unique(),displayFilename:text('display_filename').notNull(),fileSize:integer('file_size').notNull().default(0),pageCount:integer('page_count').notNull().default(0),status:text('status',{enum:['processing','ready','deleted']}).notNull()},t=>[index('idx_submissions_group_status_time').on(t.grade,t.studyDay,t.status,t.submittedAt)]);
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),expiresAt:integer('expires_at').notNull()});
export const rates=sqliteTable('rates',{key:text('key').primaryKey(),count:integer('count').notNull(),expiresAt:integer('expires_at').notNull()});
export const locks=sqliteTable('locks',{key:text('key').primaryKey(),owner:text('owner').notNull(),expiresAt:integer('expires_at').notNull()});
export const homework=sqliteTable('homework',{grade:text('grade',{enum:['GRADE_8','GRADE_9','GRADE_10']}).primaryKey(),id:text('id').notNull(),filename:text('filename').notNull(),contentType:text('content_type').notNull(),updatedAt:text('updated_at').notNull(),fileSize:integer('file_size').notNull(),storageKey:text('storage_key').notNull()});
