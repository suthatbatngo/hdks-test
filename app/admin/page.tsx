import {headers} from 'next/headers';import {redirect} from 'next/navigation';import {requireAdmin} from '@/lib/server';import Dashboard from './dashboard';export const dynamic='force-dynamic';export default async function Page(){const h=await headers();try{await requireAdmin(new Request('https://internal/admin',{headers:{cookie:h.get('cookie')||''}}))}catch{redirect('/admin/login')}return <Dashboard/>}
