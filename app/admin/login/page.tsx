"use client";import {useState} from 'react';import {LockKeyhole,ArrowRight} from 'lucide-react';import {Header,Footer} from '@/app/ui';
export default function Login(){const [password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError('');try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})}),d:any=await r.json();if(!r.ok)throw Error(d.error);setPassword('');location.assign('/admin')}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <><Header/><main><form className="panel login" onSubmit={submit}><div className="upload-icon mb-5"><LockKeyhole size={25}/></div><div className="eyebrow">DÀNH CHO GIÁO VIÊN</div><h1 className="text-2xl">ĐĂNG NHẬP QUẢN TRỊ</h1><p className="muted mt-3 mb-7 text-sm">Đăng nhập để xem và quản lý bài nộp của học sinh.</p><label className="field-label" htmlFor="password">Mật khẩu</label><input id="password" type="password" className="text-input" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required maxLength={72}/>{error&&<p className="error" role="alert">{error}</p>}<button className="primary-button w-full mt-6" disabled={busy}>{busy?'Đang đăng nhập…':'Đăng nhập'}<ArrowRight size={17}/></button><a className="block text-sm text-center muted mt-5" href="/">Quay lại trang nộp bài</a></form></main><Footer/></>}
